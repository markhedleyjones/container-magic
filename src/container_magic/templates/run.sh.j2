#!/usr/bin/env bash
# DO NOT EDIT — this file is generated from cm.yaml and will be overwritten.
# To make changes, edit cm.yaml and run: cm update
# Generated by container-magic: https://github.com/markhedleyjones/container-magic

set -euo pipefail

IMAGE_NAME="{{ project_name }}"
TAG="latest"
WORKDIR="{{ workdir }}"
WORKSPACE_NAME="{{ workspace_name }}"
SHELL="{{ shell }}"

# Detect runtime backend
{%- if backend == "auto" %}
if command -v docker >/dev/null 2>&1; then
	RUNTIME="docker"
elif command -v podman >/dev/null 2>&1; then
	RUNTIME="podman"
else
	echo "Error: Neither docker nor podman found in PATH"
	exit 1
fi
{%- else %}
RUNTIME="{{ backend }}"
{%- endif %}

# Check if image exists locally to avoid registry prompts
if ! ${RUNTIME} image inspect "${IMAGE_NAME}:${TAG}" &>/dev/null; then
	echo "Error: Image ${IMAGE_NAME}:${TAG} not found locally"
	echo "Build it with: ./build.sh"
	exit 1
fi

# Handle --stop (stop named container and exit)
if [[ "${1:-}" == "--stop" ]]; then
	${RUNTIME} stop "${IMAGE_NAME}" 2>/dev/null || true
{%- if features.display %}
{%- if backend == "auto" %}
	if [[ "${RUNTIME}" != "podman" ]]; then
		xhost -local: >/dev/null 2>&1 || true
	fi
{%- elif backend != "podman" %}
	xhost -local: >/dev/null 2>&1 || true
{%- endif %}
{%- endif %}
	exit 0
fi

# Parse --detach/-d flag (first argument only)
DETACH=false
if [[ "${1:-}" == "--detach" ]] || [[ "${1:-}" == "-d" ]]; then
	DETACH=true
	shift
fi

{%- raw %}

# Parse runtime passthrough args (everything before --)
# Usage: ./run.sh -e DEBUG=1 -v /tmp:/data -- command args
RUNTIME_PASSTHROUGH=()
_PT_CMD_ARGS=()
_PT_FOUND_SEP=false
for arg in "$@"; do
	if [[ "${_PT_FOUND_SEP}" == false ]] && [[ "$arg" == "--" ]]; then
		_PT_FOUND_SEP=true
		continue
	fi
	if [[ "${_PT_FOUND_SEP}" == true ]]; then
		_PT_CMD_ARGS+=("$arg")
	else
		RUNTIME_PASSTHROUGH+=("$arg")
	fi
done
if [[ "${_PT_FOUND_SEP}" == true ]]; then
	set -- "${_PT_CMD_ARGS[@]}"
else
	RUNTIME_PASSTHROUGH=()
fi
{%- endraw %}

# Build base run arguments (common to all commands)
BASE_RUN_ARGS=("--rm")
BASE_RUN_ARGS+=("--hostname" "${IMAGE_NAME}")
BASE_RUN_ARGS+=("--name" "${IMAGE_NAME}")
{%- if backend == "auto" %}
if [[ "${RUNTIME}" == "podman" ]]; then
	BASE_RUN_ARGS+=("--replace")
fi
{%- elif backend == "podman" %}
BASE_RUN_ARGS+=("--replace")
{%- endif %}
{%- if ipc %}
BASE_RUN_ARGS+=("--ipc" "{{ ipc }}")
{%- endif %}
{%- if privileged %}
BASE_RUN_ARGS+=("--privileged")
{%- endif %}
{%- if network %}
BASE_RUN_ARGS+=("--net" "{{ network }}")
{%- endif %}
{%- if volumes %}

# Additional bind mounts
{%- for volume in volumes %}
BASE_RUN_ARGS+=("-v" "{{ volume }}")
{%- endfor %}
{%- endif %}
{%- if devices %}

# Device passthrough
{%- for device in devices %}
BASE_RUN_ARGS+=("--device" "{{ device }}")
{%- endfor %}
{%- endif %}
{%- if features.display %}

# Display support (Wayland and X11)
XHOST_CLEANUP=false
if [[ -n "${WAYLAND_DISPLAY:-}" ]]; then
	wayland_socket="${XDG_RUNTIME_DIR}/${WAYLAND_DISPLAY}"
	BASE_RUN_ARGS+=("-e" "WAYLAND_DISPLAY")
	BASE_RUN_ARGS+=("-e" "XDG_RUNTIME_DIR=${XDG_RUNTIME_DIR}")
	BASE_RUN_ARGS+=("-v" "${wayland_socket}:${wayland_socket}:z")
fi
if [[ -n "${DISPLAY:-}" ]]; then
	xsock=/tmp/.X11-unix
{%- if backend == "auto" %}
	if [[ "${RUNTIME}" == "podman" ]]; then
		BASE_RUN_ARGS+=("--security-opt=label=disable")
		xauth=/tmp/.docker.xauth
		touch "$xauth"
		if [[ -f "$HOME/.Xauthority" ]]; then
			xauth nlist "${DISPLAY}" | sed -e 's/^..../ffff/' | xauth -f "${xauth}" nmerge -
		fi
		BASE_RUN_ARGS+=("-e" "XAUTHORITY=${xauth}")
		BASE_RUN_ARGS+=("-v" "${xauth}:${xauth}")
	else
		xhost +local: >/dev/null 2>&1
		XHOST_CLEANUP=true
	fi
{%- elif backend == "podman" %}
	BASE_RUN_ARGS+=("--security-opt=label=disable")
	xauth=/tmp/.docker.xauth
	touch "$xauth"
	if [[ -f "$HOME/.Xauthority" ]]; then
		xauth nlist "${DISPLAY}" | sed -e 's/^..../ffff/' | xauth -f "${xauth}" nmerge -
	fi
	BASE_RUN_ARGS+=("-e" "XAUTHORITY=${xauth}")
	BASE_RUN_ARGS+=("-v" "${xauth}:${xauth}")
{%- else %}
	xhost +local: >/dev/null 2>&1
	XHOST_CLEANUP=true
{%- endif %}
	BASE_RUN_ARGS+=("-e" "DISPLAY")
	BASE_RUN_ARGS+=("-v" "${xsock}:${xsock}:z")
	BASE_RUN_ARGS+=("--env" "QT_X11_NO_MITSHM=1")
fi
if [[ "${XHOST_CLEANUP}" == true ]] && [[ "${DETACH}" != true ]]; then
	trap 'xhost -local: >/dev/null 2>&1' EXIT
fi
{%- endif %}
{%- if features.gpu %}

# GPU support
if [[ -d /dev/dri ]]; then
	BASE_RUN_ARGS+=("--device" "/dev/dri:/dev/dri")
fi
if command -v nvidia-smi >/dev/null 2>&1; then
	BASE_RUN_ARGS+=("-e" "NVIDIA_DRIVER_CAPABILITIES=all")
	if [[ "${RUNTIME}" == "docker" ]]; then
		BASE_RUN_ARGS+=("--gpus=all")
	elif [[ "${RUNTIME}" == "podman" ]]; then
		BASE_RUN_ARGS+=("--device" "nvidia.com/gpu=all")
		BASE_RUN_ARGS+=("--annotation=run.oci.keep_original_groups=1")
		BASE_RUN_ARGS+=("--security-opt=label=disable")
	fi
fi
{%- endif %}
{%- if features.audio %}

# Audio support (PulseAudio/PipeWire)
if [[ -S "/run/user/$(id -u)/pulse/native" ]]; then
	BASE_RUN_ARGS+=("-v" "/run/user/$(id -u)/pulse/native:/run/user/$(id -u)/pulse/native:z")
	BASE_RUN_ARGS+=("-e" "PULSE_SERVER=unix:/run/user/$(id -u)/pulse/native")
fi
{%- endif %}
{%- if features.aws_credentials %}

# AWS credentials
if [[ -d "$HOME/.aws" ]]; then
	BASE_RUN_ARGS+=("-v" "$HOME/.aws:${WORKDIR}/.aws:z")
fi
{%- endif %}

# Load .env file if present
if [[ -f "$(pwd)/.env" ]]; then
	BASE_RUN_ARGS+=("--env-file" "$(pwd)/.env")
fi

{%- raw %}

# Runtime passthrough args
if [[ ${#RUNTIME_PASSTHROUGH[@]} -gt 0 ]]; then
	BASE_RUN_ARGS+=("${RUNTIME_PASSTHROUGH[@]}")
fi
{%- endraw %}

{%- if commands %}

# Custom command handlers
{%- for command_name, command_spec in commands.items() %}

run_{{ command_name }}() {
	{%- if command_spec.description %}
	# {{ command_spec.description }}
	{%- endif %}
	local RUN_ARGS=("${BASE_RUN_ARGS[@]}")
	local CMD="{{ command_spec.command }}"
	local EXTRA_ARGS=()

	{%- if command_spec.env %}
	# Environment variables
	{%- for key, value in command_spec.env.items() %}
	RUN_ARGS+=("-e" "{{ key }}={{ value }}")
	{%- endfor %}
	{%- endif %}
	{%- if command_spec.ports %}
	# Port publishing
	{%- for port in command_spec.ports %}
	RUN_ARGS+=("--publish" "{{ port }}")
	{%- endfor %}
	{%- endif %}
	{%- if command_spec.mounts %}

	# Parse mount arguments
	local MANIFEST_LINES=()
	local REMAINING=()
	for arg in "$@"; do
		case "$arg" in
		{%- for mount_name, mount_spec in command_spec.mounts.items() %}
		{{ mount_name }}=*)
			local _host_path="{{ '${arg#' }}{{ mount_name }}=}"
			local _resolved
			_resolved=$(realpath "$_host_path")
			{%- if mount_spec.mode == "ro" %}
			if [[ ! -e "$_resolved" ]]; then
				echo "Error: Mount '{{ mount_name }}' not found: $_host_path" >&2
				return 1
			fi
			local _basename
			_basename=$(basename "$_resolved")
			RUN_ARGS+=("-v" "${_resolved}:/mnt/{{ mount_name }}/${_basename}:ro,z")
			{%- if mount_spec.prefix %}
			{%- if mount_spec.prefix.endswith(" ") %}
			EXTRA_ARGS+=("{{ mount_spec.prefix.rstrip() }}" "/mnt/{{ mount_name }}/${_basename}")
			{%- else %}
			EXTRA_ARGS+=("{{ mount_spec.prefix }}/mnt/{{ mount_name }}/${_basename}")
			{%- endif %}
			{%- endif %}
			MANIFEST_LINES+=("${_resolved}:/mnt/{{ mount_name }}/${_basename}")
			{%- else %}
			mkdir -p "$_resolved"
			RUN_ARGS+=("-v" "${_resolved}:/mnt/{{ mount_name }}:z")
			{%- if mount_spec.prefix %}
			{%- if mount_spec.prefix.endswith(" ") %}
			EXTRA_ARGS+=("{{ mount_spec.prefix.rstrip() }}" "/mnt/{{ mount_name }}")
			{%- else %}
			EXTRA_ARGS+=("{{ mount_spec.prefix }}/mnt/{{ mount_name }}")
			{%- endif %}
			{%- endif %}
			MANIFEST_LINES+=("${_resolved}:/mnt/{{ mount_name }}")
			{%- endif %}
			;;
		{%- endfor %}
		*)
			REMAINING+=("$arg")
			;;
		esac
	done
	{%- raw %}
	local _manifest=""
	if [[ ${#MANIFEST_LINES[@]} -gt 0 ]]; then
		_manifest=$(mktemp /tmp/cm-manifest-XXXXXX)
		printf '%s\n' "${MANIFEST_LINES[@]}" > "$_manifest"
		RUN_ARGS+=("-v" "${_manifest}:/run/cm/mounts:ro,z")
	fi
	if [[ ${#REMAINING[@]} -gt 0 ]]; then
		EXTRA_ARGS+=("${REMAINING[@]}")
	fi
	{%- endraw %}
	{%- else %}
	EXTRA_ARGS=("$@")
	{%- endif %}
	{%- raw %}
	if [[ ${#EXTRA_ARGS[@]} -gt 0 ]]; then
	{%- endraw %}
		${RUNTIME} run "${RUN_ARGS[@]}" \
			-w "${WORKDIR}/${WORKSPACE_NAME}" \
			"${IMAGE_NAME}:${TAG}" \
			"${SHELL}" -c "${CMD} $(printf '%q ' "${EXTRA_ARGS[@]}")"
	else
		${RUNTIME} run "${RUN_ARGS[@]}" \
			-w "${WORKDIR}/${WORKSPACE_NAME}" \
			"${IMAGE_NAME}:${TAG}" \
			"${SHELL}" -c "${CMD}"
	fi
	{%- if command_spec.mounts %}
	{%- raw %}
	if [[ -n "$_manifest" ]]; then
		rm -f "$_manifest"
	fi
	{%- endraw %}
	{%- endif %}
}
{%- endfor %}

# Check for custom command
if [ $# -gt 0 ]; then
	case "$1" in
	{%- for command_name in commands.keys() %}
	{{ command_name }})
		shift
		run_{{ command_name }} "$@"
		exit $?
		;;
	{%- endfor %}
	esac
fi
{%- endif %}

# Use base run arguments
RUN_ARGS=("${BASE_RUN_ARGS[@]}")

if [[ "${DETACH}" == true ]]; then
	RUN_ARGS+=("--detach")
	if [ $# -gt 0 ]; then
		# Exec form: pass arguments directly (no shell wrapping).
		# Users who need shell features use: ./run.sh bash -c "cmd1 && cmd2"
		${RUNTIME} run "${RUN_ARGS[@]}" \
			-w "${WORKDIR}/${WORKSPACE_NAME}" \
			"${IMAGE_NAME}:${TAG}" \
			"$@"
	else
		${RUNTIME} run "${RUN_ARGS[@]}" \
			-w "${WORKDIR}/${WORKSPACE_NAME}" \
			"${IMAGE_NAME}:${TAG}" \
			"${SHELL}"
	fi
else
	if [ $# -gt 0 ]; then
		# Exec form: pass arguments directly (no shell wrapping)
		${RUNTIME} run "${RUN_ARGS[@]}" \
			-w "${WORKDIR}/${WORKSPACE_NAME}" \
			"${IMAGE_NAME}:${TAG}" \
			"$@"
	else
		# Interactive shell (add TTY flags only if stdin is a TTY)
		if [[ -t 0 ]]; then
			RUN_ARGS+=("--interactive" "--tty")
		fi
		${RUNTIME} run "${RUN_ARGS[@]}" \
			-w "${WORKDIR}/${WORKSPACE_NAME}" \
			"${IMAGE_NAME}:${TAG}" \
			"${SHELL}"
	fi
fi
