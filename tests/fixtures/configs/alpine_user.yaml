names:
  image: test-alpine-app
  workspace: workspace
  user: appuser

stages:
  base:
    from: alpine:latest
    steps:
      - apk: {add: [python3, py3-pip]}

  development:
    from: base

  production:
    from: base
    steps:
      - copy: workspace
