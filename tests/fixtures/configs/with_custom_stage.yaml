# Test config with custom stage
names:
  image: test-custom-stage
  workspace: workspace
  user: root

stages:
  base:
    from: debian:bookworm-slim

  development:
    from: base

  testing:
    from: base
    steps:
      - pip: {install: [pytest]}

  production:
    from: base
