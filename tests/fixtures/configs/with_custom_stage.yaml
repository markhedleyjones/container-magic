# Test config with custom stage
names:
  project: test-custom-stage
  workspace: workspace
  user: root

stages:
  base:
    from: python:3.11-slim

  development:
    from: base

  testing:
    from: base
    steps:
      - pip: {install: [pytest]}

  production:
    from: base
